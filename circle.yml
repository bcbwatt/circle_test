machine:
  environment:
    ARTEFACT: $SHORT_COMMIT
compile:
  override:
    - echo "$ARTEFACT"

test:
  override:
    - echo "nope"

deployment:
  staging:
    branch: master
    commands: 
      - echo "$CI_PULL_REQUEST"
  dev:
    branch: /.*?/
    commands:
      - echo "$CI_PULL_REQUEST"
       
